{
  "name": "cards-memory-game",
  "version": "0.0.0",
  "scripts": {
    "start": "tsc --watch & vite --host",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "vite": "^2.5.1"
  }
}
