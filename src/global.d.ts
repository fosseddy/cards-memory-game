declare global {
    interface Window {
        isDrawGrid: boolean;
        isDrawFront: boolean;
        flipSpeed: number;
        fadeoutSpeed: number;
        delayTime: number;
    }
}

export {};
